# (version removed to avoid compatibility issues)

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3457:3457"
    volumes:
      - .:/app
      - /app/node_modules